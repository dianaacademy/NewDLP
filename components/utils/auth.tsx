export const getAuthStatus = async () => {
    // Hardcoded to simulate a user being logged in or not
    const isLoggedIn = false; // Change to false to simulate a logged-out state
    return isLoggedIn;
  };